spring:
  application:
    name: eurekaservice
  cloud:
    config:
      fail-fast: true
      username: user
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false